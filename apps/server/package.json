{
  "name": "server",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "alchemy dev",
    "deploy": "alchemy deploy",
    "destroy": "alchemy destroy"
  },
  "dependencies": {
    "hono": "^4.8.2",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "zod": "catalog:",
    "@feedback-management-system/auth": "workspace:*",
    "@feedback-management-system/db": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "tsdown": "^0.16.5",
    "@feedback-management-system/config": "workspace:*",
    "alchemy": "^0.77.0",
    "wrangler": "^4.40.3",
    "@types/node": "^22.13.11",
    "@cloudflare/workers-types": "^4.20250822.0"
  }
}
